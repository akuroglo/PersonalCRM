# Настройка доступа и безопасности в Supabase

## Обзор

Этот документ описывает настройку Row Level Security (RLS) политик в Supabase для обеспечения безопасности данных. RLS гарантирует, что каждый пользователь может видеть и редактировать только свои данные, даже при прямом доступе к базе данных.

## Шаг 1: Создание таблиц в базе данных

Перед настройкой RLS политик необходимо создать таблицы. Выполните миграции Drizzle:

```bash
npm run db:push
```

Или создайте таблицы вручную через SQL Editor в Supabase, используя схему из `shared/schema.ts`.

## Шаг 2: Выполнение SQL скрипта для RLS политик

1. Откройте панель Supabase: [https://app.supabase.com](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **SQL Editor** (в левом меню)
4. Откройте файл `supabase_rls_policies.sql` из этого проекта
5. Скопируйте весь SQL код и вставьте в SQL Editor
6. Нажмите **Run** (или `Ctrl/Cmd + Enter`)

## Что делает этот скрипт?

### 1. Включает RLS для всех таблиц
- `contacts` - контакты пользователей
- `chats` - чаты с AI
- `messages` - сообщения в чатах

### 2. Настраивает политики доступа

#### Для таблицы `contacts`:
- ✅ Пользователи могут **читать** только свои контакты
- ✅ Пользователи могут **создавать** только свои контакты
- ✅ Пользователи могут **обновлять** только свои контакты
- ✅ Пользователи могут **удалять** только свои контакты

#### Для таблицы `chats`:
- ✅ Пользователи могут **читать** только свои чаты
- ✅ Пользователи могут **создавать** только свои чаты
- ✅ Пользователи могут **обновлять** только свои чаты
- ✅ Пользователи могут **удалять** только свои чаты

#### Для таблицы `messages`:
- ✅ Пользователи могут **читать** сообщения только из своих чатов
- ✅ Пользователи могут **создавать** сообщения только в своих чатах
- ✅ Пользователи могут **обновлять** сообщения только в своих чатах
- ✅ Пользователи могут **удалять** сообщения только из своих чатов

### 3. Создает индексы для производительности
- Индексы на `user_id` для быстрого поиска
- Индексы на `chat_id` для быстрого поиска сообщений
- Индексы на датах для сортировки

## Как это работает?

RLS политики используют функцию `auth.uid()` из Supabase Auth, которая возвращает ID текущего аутентифицированного пользователя. Политики автоматически применяются ко всем запросам к базе данных, включая:

- Прямые SQL запросы через Supabase клиент
- Запросы через REST API
- Запросы через GraphQL (если используется)

## Проверка работы RLS

### Тест 1: Проверка изоляции данных

1. Зарегистрируйте двух пользователей: `user1@test.com` и `user2@test.com`
2. Войдите как `user1` и создайте несколько контактов/чатов
3. Войдите как `user2` и создайте свои контакты/чаты
4. Убедитесь, что `user2` не видит данные `user1` и наоборот

### Тест 2: Проверка через SQL Editor

В SQL Editor выполните:

```sql
-- Этот запрос должен вернуть только контакты текущего пользователя
SELECT * FROM contacts;
```

Если вы не аутентифицированы, запрос вернет пустой результат или ошибку (в зависимости от настроек).

## Важные замечания

### ⚠️ Service Role Key обходит RLS

**Service Role Key** имеет полный доступ к базе данных и **обходит все RLS политики**. Это нормально для серверного кода, но:

- ✅ Используйте Service Role Key **только** на сервере
- ❌ **Никогда** не используйте Service Role Key в клиентском коде
- ❌ **Никогда** не коммитьте Service Role Key в Git

### 🔒 Anon Key соблюдает RLS

**Anon Key** (публичный ключ) соблюдает все RLS политики. Это безопасно использовать в клиентском коде.

### 📝 Текущая архитектура

В этом проекте:
- **Клиент** использует Anon Key через Supabase клиент
- **Сервер** использует Service Role Key для админских операций
- **RLS политики** защищают данные на уровне базы данных

## Отключение RLS (не рекомендуется)

Если по какой-то причине нужно временно отключить RLS:

```sql
ALTER TABLE contacts DISABLE ROW LEVEL SECURITY;
ALTER TABLE chats DISABLE ROW LEVEL SECURITY;
ALTER TABLE messages DISABLE ROW LEVEL SECURITY;
```

⚠️ **Внимание**: Это отключит всю защиту! Используйте только для отладки.

## Удаление политик

Если нужно удалить все политики:

```sql
-- Удаление всех политик для contacts
DROP POLICY IF EXISTS "Users can view their own contacts" ON contacts;
DROP POLICY IF EXISTS "Users can insert their own contacts" ON contacts;
DROP POLICY IF EXISTS "Users can update their own contacts" ON contacts;
DROP POLICY IF EXISTS "Users can delete their own contacts" ON contacts;

-- Удаление всех политик для chats
DROP POLICY IF EXISTS "Users can view their own chats" ON chats;
DROP POLICY IF EXISTS "Users can insert their own chats" ON chats;
DROP POLICY IF EXISTS "Users can update their own chats" ON chats;
DROP POLICY IF EXISTS "Users can delete their own chats" ON chats;

-- Удаление всех политик для messages
DROP POLICY IF EXISTS "Users can view messages from their own chats" ON messages;
DROP POLICY IF EXISTS "Users can insert messages in their own chats" ON messages;
DROP POLICY IF EXISTS "Users can update messages in their own chats" ON messages;
DROP POLICY IF EXISTS "Users can delete messages from their own chats" ON messages;
```

## Дополнительные настройки безопасности

### 1. Настройка CORS

В Supabase Dashboard:
- Settings → API → CORS
- Добавьте домены вашего приложения

### 2. Настройка Email провайдера

В Supabase Dashboard:
- Authentication → Providers → Email
- Настройте шаблоны писем
- Настройте Redirect URLs

### 3. Настройка Rate Limiting

В Supabase Dashboard:
- Settings → API → Rate Limiting
- Настройте лимиты запросов

## Решение проблем

### Ошибка: "new row violates row-level security policy"

**Причина**: Политика RLS блокирует операцию.

**Решение**:
1. Убедитесь, что пользователь аутентифицирован
2. Проверьте, что `user_id` в данных совпадает с `auth.uid()`
3. Проверьте, что политики созданы правильно

### Ошибка: "permission denied for table"

**Причина**: RLS включен, но политики не созданы.

**Решение**: Выполните SQL скрипт `supabase_rls_policies.sql`

### Данные не отображаются

**Причина**: RLS блокирует доступ.

**Решение**:
1. Проверьте, что пользователь аутентифицирован
2. Проверьте, что `user_id` в данных правильный
3. Проверьте логи в Supabase Dashboard → Logs

## Дополнительные ресурсы

- [Документация Supabase RLS](https://supabase.com/docs/guides/auth/row-level-security)
- [RLS Best Practices](https://supabase.com/docs/guides/auth/row-level-security#best-practices)
- [Supabase Security Guide](https://supabase.com/docs/guides/auth/security)

