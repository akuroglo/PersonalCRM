# Настройка Supabase для PersonalCRM

## Шаг 1: Создание проекта в Supabase

1. Перейдите на [https://app.supabase.com](https://app.supabase.com)
2. Войдите в свой аккаунт или создайте новый
3. Нажмите "New Project"
4. Заполните данные проекта:
   - **Name**: PersonalCRM (или любое другое имя)
   - **Database Password**: придумайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший регион
5. Дождитесь создания проекта (обычно 1-2 минуты)

## Шаг 2: Получение ключей и строки подключения Supabase

После создания проекта:

### 2.1. Получение API ключей

1. Перейдите в **Settings** → **API**
2. Найдите секцию **Project API keys**
3. Скопируйте следующие значения:
   - **Project URL** - это ваш `SUPABASE_URL`
   - **anon public** ключ - это ваш `VITE_SUPABASE_ANON_KEY`
   - **service_role** ключ - это ваш `SUPABASE_SERVICE_ROLE_KEY` (⚠️ секретный ключ!)

### 2.2. Получение строки подключения к базе данных

1. Перейдите в **Settings** → **Database**
2. Найдите секцию **Connection string**
3. Выберите режим **Session pooler** (рекомендуется) или **Direct connection**
4. Скопируйте строку подключения - это ваш `DATABASE_URL`
   - Формат: `postgresql://postgres:[YOUR-PASSWORD]@[HOST]:6543/postgres`
   - Замените `[YOUR-PASSWORD]` на пароль, который вы указали при создании проекта

## Шаг 3: Настройка переменных окружения

1. Скопируйте файл `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Откройте файл `.env` и заполните значения:

```env
# Supabase URL (скопируйте из Settings → API → Project URL)
SUPABASE_URL=https://ваш-проект.supabase.co
VITE_SUPABASE_URL=https://ваш-проект.supabase.co

# Supabase Anon Key (скопируйте из Settings → API → anon public)
VITE_SUPABASE_ANON_KEY=ваш-anon-ключ

# Supabase Service Role Key (скопируйте из Settings → API → service_role)
# ⚠️ ВНИМАНИЕ: Никогда не публикуйте этот ключ в клиентском коде!
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-ключ

# Database Connection String (скопируйте из Settings → Database → Connection string)
# Замените [YOUR-PASSWORD] на пароль вашего проекта
DATABASE_URL=postgresql://postgres:ваш_пароль@ваш-проект.supabase.co:6543/postgres

# Порт сервера (по умолчанию 5000)
PORT=5000

# OpenRouter API Key (для AI чата, опционально)
OPENROUTER_API_KEY=ваш-openrouter-ключ
```

## Шаг 4: Установка зависимостей

Убедитесь, что установлен пакет `dotenv` для загрузки переменных окружения:

```bash
npm install dotenv
```

## Шаг 5: Настройка базы данных

### 5.1. Создание таблиц

1. Выполните миграции базы данных:
   ```bash
   npm run db:push
   ```

   Или создайте таблицы вручную через SQL Editor в Supabase, используя схемы из `shared/schema.ts`.

### 5.2. Настройка Row Level Security (RLS)

⚠️ **Важно**: После создания таблиц необходимо настроить политики безопасности!

1. Откройте файл `supabase_rls_policies.sql` в проекте
2. Перейдите в **SQL Editor** в панели Supabase
3. Скопируйте весь SQL код из файла и вставьте в SQL Editor
4. Нажмите **Run** для выполнения

Это настроит политики безопасности, чтобы каждый пользователь мог видеть и редактировать только свои данные.

📖 Подробная инструкция: см. файл `SETUP_ACCESS.md`

## Шаг 6: Настройка аутентификации

1. Перейдите в **Authentication** → **Providers** в панели Supabase
2. Убедитесь, что **Email** провайдер включен
3. При необходимости настройте:
   - Email templates (шаблоны писем)
   - Redirect URLs (URL для редиректа после входа)
   - Password requirements (требования к паролю)

## Шаг 7: Проверка настройки

1. Запустите сервер разработки:
   ```bash
   npm run dev
   ```

2. Откройте браузер и перейдите на `http://localhost:5000`
3. Попробуйте зарегистрироваться или войти
4. Проверьте консоль браузера и терминал на наличие ошибок

## Важные замечания

- ⚠️ **Никогда не коммитьте файл `.env` в Git!** Он уже добавлен в `.gitignore`
- 🔒 **Service Role Key** имеет полный доступ к базе данных - храните его в секрете
- 🌐 **Anon Key** безопасно использовать в клиентском коде
- 📝 Все переменные с префиксом `VITE_` доступны в клиентском коде
- 🔐 Переменные без префикса `VITE_` доступны только на сервере

## Решение проблем

### Ошибка "Missing Supabase configuration"
- Убедитесь, что файл `.env` существует и содержит все необходимые переменные
- Проверьте, что переменные имеют правильные имена (без опечаток)
- Убедитесь, что `dotenv` установлен: `npm install dotenv`

### Ошибка подключения к базе данных
- Проверьте правильность `SUPABASE_URL`
- Убедитесь, что проект активен в Supabase
- Проверьте настройки сети/файрвола

### Ошибки аутентификации
- Проверьте настройки Email провайдера в Supabase
- Убедитесь, что `VITE_SUPABASE_ANON_KEY` правильный
- Проверьте Redirect URLs в настройках Supabase

## Дополнительные ресурсы

- [Документация Supabase](https://supabase.com/docs)
- [Supabase Auth Guide](https://supabase.com/docs/guides/auth)
- [Supabase JavaScript Client](https://supabase.com/docs/reference/javascript/introduction)

