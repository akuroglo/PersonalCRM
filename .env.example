# Supabase Configuration
# Get these values from your Supabase project dashboard: https://app.supabase.com

# Supabase URL (for both client and server)
SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_URL=your_supabase_project_url

# Supabase Anon Key (public key for client-side)
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Service Role Key (secret key for server-side admin operations)
# WARNING: Never expose this key in client-side code!
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Server Port (optional, defaults to 5000)
PORT=5000

# OpenRouter API Key (for AI chat functionality)
OPENROUTER_API_KEY=your_openrouter_api_key

# Database Connection String (PostgreSQL)
# Get this from Supabase Dashboard → Settings → Database → Connection string
# Use "Session pooler" mode (port 6543) or "Direct connection" (port 5432)
# Format: postgresql://postgres:[YOUR-PASSWORD]@[HOST]:6543/postgres
DATABASE_URL=postgresql://postgres:your_password@your_project_ref.supabase.co:6543/postgres
